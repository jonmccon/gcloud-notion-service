{
  "name": "gcloud-notion-service",
  "image": "mcr.microsoft.com/devcontainers/python:3.11",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance"
      ]
    }
  },
  "postCreateCommand": "bash -c 'curl -sSL https://sdk.cloud.google.com | bash -s -- --disable-prompts --install-dir=/home/vscode && echo \"source /home/vscode/google-cloud-sdk/path.bash.inc\" >> /home/vscode/.bashrc && pip install --upgrade pip && pip install -r requirements.txt'",
  "remoteUser": "vscode"
}
